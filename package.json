{
  "name": "alem-tasks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host :: --port 5173 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host ::",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
